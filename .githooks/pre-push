#!/bin/sh
echo "pre-push: running e2e tests (skip with SKIP_E2E=1 or git push --no-verify)"
[ "$SKIP_E2E" = "1" ] && exit 0
npm run test:e2e || {
  echo "pre-push: e2e tests failed, push aborted"
  exit 1
}
